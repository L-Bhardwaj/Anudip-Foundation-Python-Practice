# Verify if a number is in the range of 1 to 100 or outside

num=int(input('Enter the number: '))
if num>=1 and num<=100:
    print(num, ' is in the range.')
else:
    print(num, ' is not in the range.')